apiVersion: v1
kind: Secret
metadata:
  name: ecojoko-secret
  namespace: default
type: Opaque
stringData:
  MQTT_HOST: "192.168.1.205"
  MQTT_PASSWORD: "${MQTT_PASSWORD}"
  MQTT_USERNAME: "${MQTT_USER}"
  ECOJOKO_USERNAME: "${ECOJOKO_USERNAME}"
  ECOJOKO_PASSWORD: "${ECOJOKO_PASSWORD}"
