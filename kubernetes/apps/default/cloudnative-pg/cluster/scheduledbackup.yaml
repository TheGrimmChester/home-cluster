---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: general-purpose-scheduled-backup
  namespace: default
spec:
  schedule: "@daily"
  immediate: true
  backupOwnerReference: self
  cluster:
    name: postgres
