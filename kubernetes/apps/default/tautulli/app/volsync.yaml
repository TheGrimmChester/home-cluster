#---
#apiVersion: v1
#kind: Secret
#metadata:
#  name: vaultwarden-restic-secret
#  namespace: default
#type: Opaque
#stringData:
#  RESTIC_REPOSITORY: ${SECRET_RESTIC_REPOSITORY_BASE}/vaultwarden
#  RESTIC_PASSWORD: ${SECRET_RESTIC_PASSWORD}
#  AWS_ACCESS_KEY_ID: ${SECRET_RESTIC_ACCESS_KEY_ID}
#  AWS_SECRET_ACCESS_KEY: ${SECRET_RESTIC_SECRET_ACCESS_KEY}

---
# yaml-language-server: $schema=https://kubernetes-schemas.devbu.io/replicationsource_v1alpha1.json
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: tautulli
  namespace: default
spec:
  sourcePVC: config-tautulli-0
  trigger:
    schedule: "*/30 * * * *"
  restic:
    copyMethod: Snapshot
    pruneIntervalDays: 10
    repository: tautullli-secret
    cacheCapacity: 2Gi
    volumeSnapshotClassName: csi-ceph-blockpool
    storageClassName: ceph-block
    moverSecurityContext:
      runAsUser: 568
      runAsGroup: 568
      fsGroup: 568
#    retain:
#      daily: 10
#      within: 3d
    retain:
      hourly: 1
      daily: 1
      weekly: 1
      monthly: 1
      yearly: 1
